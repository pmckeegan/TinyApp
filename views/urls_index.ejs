<!DOCTYPE html>
<html lang="en">

<head>
	<meta charset="utf-8">
	<meta name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no">
	<link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.2.1/css/bootstrap.min.css" 
				integrity="sha384-GJzZqFGwb1QTTN6wy59ffF1BuGJpLSa9DkKMp0DgiMDm4iYMj70gZWKYbI706tWS" crossorigin="anonymous">
	
	<title>TinyApp</title>
	<header>
		<% include ./partials/_header.ejs %>
	</header>

</head>

<body style="font-size:20px; padding:50px;">
	<ul>
	
	<% for (let shortURL in urls){ %>
			
			<li><%= shortURL %> <a href="/u/<%= urls[shortURL] %>"></a> - 
			 <a href="<%= urls[shortURL].longURL %>"> <%= urls[shortURL].longURL %></a></li>
				<div>
				<form method="POST" action="/urls/<%= shortURL %>/update">
					<button type="submit" class="btn btn-primary">Update</button>
				</form>
				<form method="POST" action="/urls/<%= urls %>/delete">
					<button type="submit" class="btn btn-danger">Delete Link</button>
					</form>
					</div>
				<form class="form-inline" method="POST" action="/urls/<%= urls %>/" >
					<div class="form-group mb-2">
					<input class="form-control" type="text" name="longURL" 
					placeholder="http://" style="width: 800px; margin: 1em">
				</div>
			</form>
				</form>
			
	<% } %>
	</ul>
</body>
</html>